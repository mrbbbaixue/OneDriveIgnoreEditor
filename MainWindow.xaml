<Window
    x:Class="OneDriveIgnoreEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="OneDriveIgnoreEditor"
    Width="600"
    Height="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,10"
                FontSize="20"
                FontWeight="Bold"
                Text="OneDriveIgnoreEditor" />
            <DataGrid
                x:Name="IgnoreDataGrid"
                Grid.Row="1"
                Margin="0,0,0,0"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CellEditEnding="IgnoreDataGrid_CellEditEnding"
                HeadersVisibility="Column"
                ItemsSource="{Binding IgnoreRules}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Rule, UpdateSourceTrigger=PropertyChanged}"
                        Header="规则" />
                    <DataGridTemplateColumn Width="70" Header="操作">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Content="删除" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel
                Grid.Row="2"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="80"
                    Margin="5"
                    Click="SaveButton_Click"
                    Content="保存" />
                <Button
                    Width="80"
                    Margin="5"
                    Click="RefreshButton_Click"
                    Content="刷新" />
                <Button
                    Width="100"
                    Margin="5"
                    Click="ImportButton_Click"
                    Content="从文件导入" />
            </StackPanel>
        </Grid>
        <StatusBar Grid.Row="1">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="准备就绪" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>